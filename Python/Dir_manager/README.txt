Script manage the content of specified directory by moving files with specific extension to corresponding subdirectory.

Mapping between file extensions and subdirectories names should be passed to the script as simple config *.json file,
where keys represents the file extension and values are the names of subdirectories.
Path to directory, on which script will be operating should be passed as a script argument.