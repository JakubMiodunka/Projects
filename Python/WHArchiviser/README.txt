Archivizer of time spend at work.

Each day is represented by a *.json file stored in an archive directory.
Archive will be created in the same directory where script file is stored as a sepparete directory caled 'Archive'.
Archive structure: Archive/<company name>/<year>/<date in format YYYY-MM-DD>.json