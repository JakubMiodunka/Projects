First trial of socket programming in Python.
Aim was to create client-server application launched via CLI, where many users can communicate with each other using sockets.

For now there are only a fiew classes utilised basic functionalities of the program.

Application uses only build-in Python packages.